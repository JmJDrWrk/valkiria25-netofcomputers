// Example of a correctly exported React component
import React from 'react';
import { Typography } from '@mui/material';

function SongToTracks() {
  return (
    <Typography variant="h4" gutterBottom>
      Split a song into different instruments using AI
    </Typography>
  );
}

export default SongToTracks;  // Make sure you're using default export
